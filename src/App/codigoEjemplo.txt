int _x = 0;
int main () {
    for (_x = 5; x < 10; _x = _x + 1)
        y = exec(y) + exec(z);
    if (y < 100) {
        y = y / 2;
        z = y - 5.5;
    }
    else
        z = y + 5.5;

    return 0;
}

double exec (double valor) {
    int i = 10;

    while ((i >= 5) && (valor > 3.4))
        valor -= _x - i;

    return valor;
}
